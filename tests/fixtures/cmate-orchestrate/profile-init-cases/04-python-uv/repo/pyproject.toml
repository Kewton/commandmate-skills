[project]
name = "sprocket"
version = "0.1.0"
requires-python = ">=3.12"

[project.urls]
Repository = "https://github.com/Acme/sprocket"

[tool.ruff]
line-length = 100

[tool.mypy]
strict = true

[tool.pytest.ini_options]
testpaths = ["tests"]
