# Generated by cmate-orchestrate (dispatch runner) from an approved plan.
# Do not edit by hand: the same plan regenerates this file byte for byte.
version: 1
title: "#361 feat: resolve API keys by declared scope"
goal: |
  # Issue #361 — feat: resolve API keys by declared scope

  Repository: Kewton/CommandMate
  Base branch: origin/develop
  Work branch: feature/issue-361-feat-resolve-api-keys-by-declared-scope
  Worktree: ../CommandMate-issue-361-feat-resolve-api-keys-by-declared-scope
  Issue body: `gh issue view 361` — the authority. This goal is a summary of it.

  ## Objective
  API キーの取り扱いを、鍵ごとの scope 宣言つきに置き換える。

  ## Prohibitions and constraints — transcribed from the issue body
  これは要約ではない。以下は Issue 本文からの原文転記である。
  A prohibition this contract does not state is NOT a permission: it is text the
  summary did not carry. The issue body is the authority.
  契約が言及していない禁止事項は、契約が許可したのではなく書いていないだけである。

  [原文転記] 本文「## 禁止事項」の節
  | してはいけないこと | 理由 |
  | --- | --- |
  | 鍵の平文を log へ出す | log は集約先へ複製されるので、消しても消えない |
  | 鍵を error message へ載せる | error は telemetry へ送られ、外部へ出る |
  | 鍵を test fixture へ hardcode する | fixture は履歴に残るので、後から取り除けない |
  | 鍵を環境変数名ごと dump する | dump は support 経路で共有され、受け手を選べない |
  | 期限切れの鍵を silent に再発行する | 失効したことを誰も観測できなくなる |
  | scope 宣言の無い鍵を既定で通す | 宣言忘れが最大権限になり、既定が最も危険になる |
  | 鍵の比較を非定数時間で行う | 比較時間から鍵の先頭が漏れる |
  | 鍵を URL の query へ載せる | query は proxy と referer に残る |
  | 鍵を clipboard へ書き出す | clipboard は他 process から読めるので、範囲を選べない |
  | 鍵を 3rd party の SDK へそのまま渡す | 渡した先の保存先を、こちらから観測できない |
  | 鍵を含む request を retry log へ残す | retry log は保持期間が別に設定されており、消し忘れる |
  | 鍵の生成に予測可能な乱数源を使う | 生成器が分かれば鍵空間が縮む |
  | 鍵の scope を実行時に拡張する | 実行時の拡張は宣言と食い違い、宣言が意味を失う |
  | 失効 API の失敗を warning で流す | 失効できていないことが、成功と見分けられなくなる |
  | 鍵を worktree の生成物へ書き出す | 生成物は artifact として外へ持ち出される |
  | 鍵の一覧を管理画面へそのまま表示する | 画面は screenshot になり、共有先を選べない |
  | 鍵の hash を識別子として再利用する | hash から元の鍵を照合する経路が生まれる |
  | 鍵の scope 判定を client 側だけで行う | client の判定は差し替えられる |

  転記しきれず落とした: 「## セキュリティ上の考慮」の節（over_budget）、「## テスト方針」の節（over_budget）。
  本文に他節がある。`gh issue view 361` で全文を読め

  ## Acceptance criteria
  - `src/auth/keyring.ts` resolves a key by its declared scope
  - a key whose scope is not declared is rejected

  ## Files you may change
  - src/auth/keyring.ts
  - src/auth/keyring.test.ts
  - src/auth/keyring.spec.ts
  - src/auth/__tests__/keyring.ts
  - src/auth/__tests__/keyring.test.ts

  ## Rules
  - A prohibition this contract does not state is NOT a permission — it is text the
    summary did not carry. Read the issue body (read-only) before you decide
    anything it might constrain. 契約が言及していない禁止事項は、許可ではない。
  - Stay within this issue. Do not modify files another issue in the plan owns.
  - The completion criterion above is the contract's, not a suggestion: run those
    commands yourself and make them pass before reporting done. Do not report done
    on a failing gate — the same gates decide the verdict.
  - Keep working across turns until the whole task is finished; do not stop half-done.
  - When the work is complete, make a SINGLE commit of this issue's changes on the
    work branch. Verification can pass on uncommitted work, but nothing downstream
    can deliver it, so the commit is what ends the task.
  - If a step is destructive, ambiguous, or blocked, STOP and ask. Do not guess.
  - Do not print tokens, secrets, or absolute host paths.
scope:
  allow:
    - "src/auth/__tests__/keyring.test.ts"
    - "src/auth/__tests__/keyring.ts"
    - "src/auth/keyring.spec.ts"
    - "src/auth/keyring.test.ts"
    - "src/auth/keyring.ts"
  deny: []
autoYes:
  mode: "off"
success:
  requireWorkEvidence: true
  requireScopeClean: true
  autoVerifyOnStop: false
