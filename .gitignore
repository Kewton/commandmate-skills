node_modules/
dist/
*.tgz
*.tar.gz
.DS_Store

# Python bytecode from the release pipeline scripts
__pycache__/
*.py[cod]

# Worker task contracts generated by cmate-orchestrate dispatch (never committed;
# .commandmate/verify.yaml itself is tracked)
.commandmate/tasks/
# Orchestration scratch written by workers (CHANGELOG / module-reference fragments,
# QA notes). CommandMate's own repo ignores the same path; this repo did not, so
# PR #240 committed two of them by accident (Issue #236 cleanup).
dev-reports/
