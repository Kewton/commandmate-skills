{
  "description": "A worker that idles forever without ever committing is not silently accepted. With --max-turns 3, #201 is driven three turns (initial + two nudges); with no new commit the supervisor gives up and reports the worker as failed. The wave barrier holds and #200 is never dispatched — an honest stop, not a false completion (Issue #1468).",
  "plan": {
    "issues_fixture": "cases/02-explicit-dependency/issues.json",
    "orchestrate_args": ["200", "201", "--profile", "node-commandmate", "--max-parallel", "3", "--run-id", "plan"]
  },
  "scenario": "scenario.json",
  "dispatch_args": ["--max-turns", "3"],
  "expect": {
    "exit": 7,
    "status": "partial",
    "stop_reason": "worker_failed",
    "human_required": false,
    "waves_count": 1,
    "never_sent": [200],
    "no_respond": true,
    "send_counts": { "201": 3 },
    "worker_states": { "201": "failed" }
  }
}
