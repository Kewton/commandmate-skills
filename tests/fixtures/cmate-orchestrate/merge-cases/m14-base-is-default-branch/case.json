{
  "description": "Issue #39: the plan's base IS the repository's default branch, so `Resolves #n` will auto-close on merge. Nothing is wrong and nothing is recorded — the limitation is not raised speculatively.",
  "phase": "create-prs",
  "plan": {
    "issues_fixture": "cases/02-explicit-dependency/issues.json",
    "orchestrate_args": ["200", "201", "--profile", "node-commandmate", "--max-parallel", "3", "--run-id", "plan"]
  },
  "merge_args": ["--approve"],
  "merge_scenario": {
    "cli_available": true,
    "gh": { "repo_access": true, "default_branch": "develop" },
    "git": { "base_resolvable": true },
    "prs": {
      "200": { "create": "ok" },
      "201": { "create": "ok" }
    }
  },
  "expect": {
    "exit": 0,
    "status": "success",
    "stop_reason": "completed",
    "approved": true,
    "mutated": true,
    "eligible": [201, 200],
    "targets_outcome": { "201": "pr_created", "200": "pr_created" },
    "pr_create_calls": 2,
    "absent_limitation_codes": ["issue_autoclose_not_default_branch"],
    "pr_body_contains": ["Resolves #201."],
    "pr_body_absent": ["not the default branch"],
    "completion_passed": true
  }
}
